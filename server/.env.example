# SecondMe 配置文件
# 复制此文件为 .env 并填写实际配置

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8060

# ========== 认证配置 ==========
# JWT 密钥（生产环境请使用随机字符串）
JWT_SECRET=change-this-secret-key-in-production

# Token 有效期（小时），默认 168 小时 = 7 天
# 滑动过期：当剩余有效期不足一半时自动刷新
JWT_EXPIRE_HOURS=168

# 首次启动时创建的管理员账号
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ========== AI Provider 配置 ==========
# 默认 AI Provider 配置（启动时自动创建）
DEFAULT_PROVIDER_NAME=OpenAI
DEFAULT_PROVIDER_BASE_URL=https://api.openai.com/v1
DEFAULT_PROVIDER_API_KEY=sk-your-api-key-here

# 默认模型配置
DEFAULT_CHAT_MODEL=gpt-3.5-turbo
DEFAULT_EMBEDDING_MODEL=text-embedding-3-small

# ========== 记忆配置 ==========
DEFAULT_MEMORY_TOP_K=5
